import React from 'react';
import SearchFood from './SearchFood'

const AddToMealForm = () => {
    return (
        <div>
            <SearchFood/>
        </div>
    );
}

export default AddToMealForm;
